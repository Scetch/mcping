[package]
name = "mcping"
version = "0.1.0"
license = "MIT OR Apache-2.0"
authors = ["Scetch <me@scet.ch>"]
edition = "2018"

[dependencies]
byteorder = "1.3"
dns-lookup = "1.0"
rand = "0.7"
serde = { version = "1.0", features = [ "derive" ] }
serde_json = "1.0"
thiserror = "1.0"

[dev-dependencies]
dialoguer = "0.6"
